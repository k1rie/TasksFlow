# Configuración de la base de datos MySQL
DB_HOST=https://alphatech-labs.com/
DB_USER=alphate2_smartclass_admin
DB_PASSWORD=HnhOS,,}eVe&
DB_NAME=alphate2_smartclass
DB_PORT=3306

# Configuración del servidor
PORT=8000
NODE_ENV=development

# Configuración de CORS (opcional)
CORS_ORIGIN=http://localhost:3000

# Configuración de email (si usas nodemailer)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=tu_email@gmail.com
EMAIL_PASSWORD=tu_app_password_aqui

# JWT Secret (si usas autenticación)
JWT_SECRET=tu_jwt_secret_muy_seguro_aqui

# Configuración de archivos (si usas multer)
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=5242880
